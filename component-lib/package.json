{
  "name": "component-lib",
  "version": "1.0.0",
  "description": "react component library",
  "main": "dist/index.js",
  "scripts": {
    "test": "jest",
    "build:watch": "tsc --project tsconfig.json --pretty --watch",
    "build": "rm -rf dist; tsc --project tsconfig.json --pretty"
  },
  "keywords": [
    "react",
    "component",
    "library"
  ],
  "peerDependencies": {
    "react": ">17.0.0"
  },
  "author": "avimehenwal",
  "license": "ISC",
  "devDependencies": {
    "@types/react": "^17.0.0",
    "react": "^17.0.1",
    "typescript": "^4.1.3"
  }
}